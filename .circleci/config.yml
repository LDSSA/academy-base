version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run: docker build -t ldssa/academy-base:latest .
      - run: docker push ldssa/academy-base:latest
